server:
  port: 8081
  #disabling whitelabel in application
#  error:
#    whitelabel:
#      enabled: false
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwtdemo
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
#config for disabling ResourceHttpRequestHandler,render method whitelabel
#  mvc:
#    throw-exception-if-no-handler-found: true
#  resources:
#    add-mappings: false

    
    
    